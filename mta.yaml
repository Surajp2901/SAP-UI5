_schema-version: "3.2"
ID: soapp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: soapp-destination-content
  type: com.sap.application.content
  requires:
  - name: soapp-destination-service
    parameters:
      content-target: true
  - name: soapp-repo-host
    parameters:
      service-key:
        name: soapp-repo-host-key
  - name: soapp-uaa
    parameters:
      service-key:
        name: soapp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: soapp_html_repo_host
          ServiceInstanceName: soapp-html5-service
          ServiceKeyName: soapp-repo-host-key
          sap.cloud.service: soapp
        - Authentication: OAuth2UserTokenExchange
          Name: soapp_uaa
          ServiceInstanceName: soapp-xsuaa-service
          ServiceKeyName: soapp-uaa-key
          sap.cloud.service: soapp
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: soapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: soapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - soapp.zip
      name: soapp
      target-path: resources/
- name: soapp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: soapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: soapp-destination-service
    service-plan: lite
- name: soapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: soapp-xsuaa-service
    service-plan: application
- name: soapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: soapp-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
